# Data structure
- `event:{id}` -> json
- `org:{id}` -> json
- `org-ops`, `{email}` -> level
- `user:{email}` -> json
- `user-fav` `{email}` -> string
- `auth-flow`, `{state}` -> {nonce}
- `auth-session`, `{session}` -> {email}
